# Variables de entorno para producción (Vercel)
# Copiar estas variables en el dashboard de Vercel: Settings → Environment Variables

# Base de datos PostgreSQL (se obtiene automáticamente al crear Vercel Postgres)
POSTGRES_URL=
POSTGRES_PRISMA_URL=
POSTGRES_URL_NON_POOLING=

# NextAuth.js
NEXTAUTH_URL=https://concurso-fotografia-sav.vercel.app
NEXTAUTH_SECRET=genera_con_openssl_rand_base64_32

# Vercel Blob Storage (se obtiene al crear Vercel Blob)
BLOB_READ_WRITE_TOKEN=

# Para generar NEXTAUTH_SECRET localmente:
# openssl rand -base64 32